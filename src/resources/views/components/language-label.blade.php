<div>
    @if($currentLanguage)
        <span class="ml-1 text-xs inline-flex items-center bg-indigo-400 text-white px-1 rounded">
            {{strtoupper($currentLanguage)}}
        </span>
    @endif
</div>
